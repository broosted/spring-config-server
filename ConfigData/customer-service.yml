---
  spring:
    application:
      name: config-server
  server:
    port: 8000
  spring:
    jpa:
      hibernate:
        ddl-auto: update
  spring:
    datasource:
      url: jdbc:mysql://localhost:3306/company
  spring:
    datasource:
      username: root
  spring:
    datasource:
      password: root
